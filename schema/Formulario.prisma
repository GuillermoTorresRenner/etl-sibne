//REVISADO
model Formulario {
  id                     Int                     @id @default(autoincrement())
  nombre                 String
  orden                  Int
  codigoNav              String

  categoriaTransacciones CategoriaTransaccion[]
  transacciones           Transaccion[]

  @@map("Formulario")
}