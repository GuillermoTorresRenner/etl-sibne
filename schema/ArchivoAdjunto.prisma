//REVISADO
model ArchivoAdjunto {
  id                Int       @id @default(autoincrement())
  nombreArchivo     String
  tipo              String
  ext               String
  FileName        String

  encuestas         Encuesta[] @relation("ArchivoAdjuntoEntidad")
  // Si solo una encuesta puede tener un archivo adjunto, usa:
  // encuesta Encuesta?
  @@map("ArchivoAdjunto")
}