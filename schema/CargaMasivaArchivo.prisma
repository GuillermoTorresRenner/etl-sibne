//REVISADO
model CargaMasivaArchivo {
  id             Int                  @id @default(autoincrement())
  nombreArchivo  String
  fechaIngreso   DateTime?
  estado         String

  cargaMasivaDetalle CargaMasivaDetalle[]

  @@map("CargaMasivaArchivo")
}