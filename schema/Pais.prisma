// REVISADO
model Pais {
  id         Int    @id @default(autoincrement())
  iso2       String
  iso3       String
  nombre     String
  numeroIso  Int
  transacciones Transaccion[]

  @@map("Pais")
}